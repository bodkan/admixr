# Download example SNP data.

The data is downloaded to a temporary directory by default.

## Usage

``` r
download_data(dirname = tempdir())
```

## Arguments

- dirname:

  Directory in which to put the data (EIGENSTRAT trio of snp/geno/ind
  files).
